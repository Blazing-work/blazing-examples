# Expected Output

## Running

```bash
python flow.py
```

## Output

```
[1, 3, 5]
```

## Notes

- Result of `get_positive_numbers(numbers=[1, -2, 3, -4, 5])` workflow
- Filters out negative numbers (-2 and -4)
- Returns only positive values in original order
- Sync version produces identical results
