version: v0.2
project_id: '{{PROJECT_ID}}'
cluster_id: '{{CLUSTER_ID}}'
cluster:
  name: privategpt-gpu
providers:
  akash:
    status: prefer
services:
  privategpt:
    image: 3x3cut0r/privategpt:latest
    cpu: 4.0
    memory: 16Gi
    storage:
      size: 100Gi
    network:
      ingress:
        port: 8080
        public: true
