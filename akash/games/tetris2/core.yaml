version: v0.2
project_id: '{{PROJECT_ID}}'
cluster_id: '{{CLUSTER_ID}}'
cluster:
  name: tetris2
providers:
  dcf:
    status: prefer
  akash:
    status: fallback
services:
  tetris:
    image: uzyexe/tetris
    cpu: 1.0
    memory: 512Mi
    storage:
      size: 512Mi
    network:
      ingress:
        port: 80
        public: true
