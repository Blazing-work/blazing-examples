version: v0.2
organization_id: '{{ORG_ID}}'
project_id: '{{PROJECT_ID}}'
cluster_id: '{{CLUSTER_ID}}'

cluster:
  name: tetris2-multi

providers:
  dcf:
    status: prefer
  akash:
    status: fallback
    max_price: 15.00
  gcp:
    status: prefer
    network_tier: Standard
    ip_version: IPV4

regions:
  mode: explicit
  explicit:
    - region_id: akash-global
      provider: akash
      is_primary: true
    - region_id: gcp-europe-west1
      provider: gcp
      network_tier: Standard
      ip_version: IPV4
    - region_id: dfc-pt-lisbon
      provider: dfc

services:
  tetris:
    image: uzyexe/tetris
    cpu: 1.0
    memory: 512Mi
    storage:
      size: 512Mi
    mesh:
      enabled: false
    network:
      ingress:
        port: 80
        public: true
